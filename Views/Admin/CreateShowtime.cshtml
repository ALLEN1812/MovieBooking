@model ShowtimeFormViewModel
@{
    ViewData["Title"] = "Thêm suất chiếu";
    ViewData["ActiveMenu"] = "Showtimes";
    Layout = "_AdminLayout";
}

<div class="card border-0 shadow-sm" style="max-width:600px;">
    <div class="card-body p-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2"></div>
            <div class="row g-3">
                <div class="col-12">
                    <label asp-for="MovieId" class="form-label fw-semibold">Phim <span class="text-danger">*</span></label>
                    <select asp-for="MovieId" class="form-select" asp-items="@(new SelectList(Model.Movies, "Id", "Title"))">
                        <option value="">-- Chọn phim --</option>
                    </select>
                    <span asp-validation-for="MovieId" class="text-danger small"></span>
                </div>
                <div class="col-12">
                    <label asp-for="RoomId" class="form-label fw-semibold">Phòng chiếu <span class="text-danger">*</span></label>
                    <select asp-for="RoomId" class="form-select" asp-items="@(new SelectList(Model.Rooms, "Id", "Name"))">
                        <option value="">-- Chọn phòng --</option>
                    </select>
                    <span asp-validation-for="RoomId" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="StartTime" class="form-label fw-semibold">Giờ chiếu <span class="text-danger">*</span></label>
                    <input asp-for="StartTime" class="form-control" type="datetime-local" />
                    <span asp-validation-for="StartTime" class="text-danger small"></span>
                </div>
                <div class="col-md-6">
                    <label asp-for="Price" class="form-label fw-semibold">Giá vé (₫)</label>
                    <input asp-for="Price" class="form-control" type="number" />
                </div>
                <div class="col-12">
                    <label asp-for="Subtitle" class="form-label fw-semibold">Phụ đề / Thuyết minh</label>
                    <select asp-for="Subtitle" class="form-select">
                        <option value="Vietsub">Vietsub</option>
                        <option value="Thuyết minh">Thuyết minh</option>
                        <option value="Phụ đề Anh">Phụ đề Anh</option>
                    </select>
                </div>
            </div>
            <div class="d-flex gap-2 mt-4">
                <button type="submit" class="btn btn-danger"><i class="bi bi-save me-2"></i>Lưu</button>
                <a asp-action="Showtimes" class="btn btn-outline-secondary">Hủy</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
